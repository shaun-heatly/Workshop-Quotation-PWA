/** @type {import('next').NextConfig} */
const nextConfig = {
  // No PWA/service worker configuration needed
  // We're using File System Access API + localStorage directly
};

module.exports = nextConfig;